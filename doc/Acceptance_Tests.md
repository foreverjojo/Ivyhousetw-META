1. Same FP 不新增：同檔案連跑快篩 2 次 → versions 不增加

2. Quick→Final 同 vdir：先 BCD，再 BCEF → 同 fp 資料夾只多出 E/F 產物

3. Mismatch + Auto：latest.fp != current → Auto 新建 fp 資料夾

4. Mismatch + No Auto/No Force：應停下並提示 mismatch

5. LLM 不重算數字：insights/notes 只能引用 report_summary 的數字，不得生成另一套 KPI