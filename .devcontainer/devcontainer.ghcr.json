{
  "name": "Ivyhousetw-META (GHCR pinned devcontainer image)",
  "image": "ghcr.io/foreverjojo/ivyhousetw-meta-devcontainer:devcontainer-main",
  "features": {},
  "containerEnv": {
    "UV_VERSION": "0.5.24"
  },
  "remoteEnv": {
    "OAI_API_KEY": "${localEnv:OAI_API_KEY}",
    "OAI_BASE_URL": "${localEnv:OAI_BASE_URL}",
    "OPENROUTER_API_KEY": "${localEnv:OPENROUTER_API_KEY}"
  },
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash",
        "python.defaultInterpreterPath": "/workspaces/Ivyhousetw-META/.venv/bin/python",
        "python.terminal.activateEnvironment": false
      },
      "extensions": [
        "alefragnani.project-manager",
        "bierner.color-info",
        "chouzz.vscode-better-align",
        "christian-kohler.path-intellisense",
        "continue.continue",
        "dbaeumer.vscode-eslint",
        "eamodio.gitlens",
        "emanuelebartolesi.vscode-copilot-insights",
        "esbenp.prettier-vscode",
        "github.copilot",
        "github.copilot-chat",
        "github.vscode-github-actions",
        "github.vscode-pull-request-github",
        "gruntfuggly.todo-tree",
        "gxl.git-graph-3",
        "johnpapa.vscode-peacock",
        "louiswt.regexp-preview",
        "mechatroner.rainbow-csv",
        "ms-azuretools.vscode-containers",
        "ms-python.autopep8",
        "ms-python.debugpy",
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-python.vscode-python-envs",
        "oderwat.indent-rainbow",
        "openai.chatgpt",
        "rangav.vscode-thunder-client",
        "ritwickdey.liveserver",
        "streetsidesoftware.code-spell-checker",
        "usernamehw.errorlens",
        "vscode-icons-team.vscode-icons",
        "yzhang.markdown-all-in-one"
      ]
    }
  },
  "remoteUser": "vscode",
  "workspaceFolder": "/workspaces/Ivyhousetw-META",
  "mounts": [
    "source=${localWorkspaceFolder},target=/workspaces/Ivyhousetw-META,type=bind,consistency=cached"
  ],
  "postCreateCommand": "bash -lc 'set -euo pipefail; cd /workspaces/Ivyhousetw-META; if [ ! -d .venv ]; then python -m venv .venv; fi; . .venv/bin/activate; python -m pip install -U pip; UVV=\"${UV_VERSION:-0.5.24}\"; if command -v uv >/dev/null 2>&1; then cur=\"$(uv --version 2>/dev/null | awk \"{print \\$2}\" || true)\"; if [ -n \"$cur\" ] && [ \"$cur\" != \"$UVV\" ]; then python -m pip install --no-cache-dir \"uv==${UVV}\"; fi; else python -m pip install --no-cache-dir \"uv==${UVV}\"; fi; if [ -f uv.lock ]; then uv sync --frozen; else uv pip install -r requirements.txt; fi'",
  "runArgs": [
    "--add-host=host.docker.internal:host-gateway"
  ]
}

